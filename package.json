{
  "name": "carelink",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de monitoramento e assistência integrada a uma pulseira inteligente.",
  "workspaces": [
    "frontend",
    "simulador-pulseira"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend"
  },
  "engines": {
    "node": ">=20"
  }
}
